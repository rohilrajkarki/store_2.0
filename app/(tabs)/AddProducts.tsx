import { ThemedText } from "@/components/ThemedText";
import React from "react";
import { View } from "react-native";

const AddProducts = () => {
  return (
    <View>
      <ThemedText>AddProducts</ThemedText>
    </View>
  );
};

export default AddProducts;
